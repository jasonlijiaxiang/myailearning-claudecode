# 原始输入留痕

- **日期**：2026-07-09
- **本次动作**：更新已有模块（B 类）—— 把模块预留的可增补方向做进去，新增第 10–12 章
  （GraphRAG、多模态 RAG、结构化数据 RAG / Text-to-SQL）。只增不改，沿用现有结构风格。

---

## 用户原始输入（原样留存）

> 更新一下 这个RAG 模块

## 路由判断

- 输入粗略，但明确指向已有 RAG 模块 → B 类。
- 模糊点（增补剩余预留方向 vs 刷新时效性 vs 定点改某章）经反问消除：
  用户拍板"增补剩余预留方向"。
- 剩余预留方向来自模块 README 的"可增补方向"卡：GraphRAG、多模态 RAG、
  结构化数据 RAG（Text-to-SQL）。

## 从输入提炼出的子主题（待第 3 步方案确认）

1. GraphRAG：知识图谱增强检索，全局性问题（"总结全部""横向对比"）的解法
2. 多模态 RAG：图表/图片/音视频进检索管线，多模态 embedding 与解析路线
3. 结构化数据 RAG：Text-to-SQL / 语义层，让 RAG 会"查数"而不只"查文"

## 最终产出（2026-07-09 完成）

- **PPT**：`RAG-讲义.pptx` 92 页 → 119 页。新增第 10–12 章（进阶篇）：
  ⑩ GraphRAG（全局盲区、四步索引、成本账与 LazyGraphRAG、向量+图谱路由）
  ⑪ 多模态 RAG（三条路线、ColPali/MaxSim、选型口诀）
  ⑫ 结构化数据 RAG（查文 vs 查数、裸 Text-to-SQL 静默错误、语义层口径手册）。
  既有 9 章一字未动；导览页改三栏（原理/实践/进阶），总收束扩至 12 条，
  全书串联页三条候选转正（Evaluation / Fine-tuning / LLM 原理链接由巡检会话先行加入）。
- **书单**：15 → 20 份（GraphRAG 论文、MS GraphRAG 文档、ColPali 论文、
  HF ColPali+Milvus 教程、dbt 语义层基准）；2 篇 arXiv PDF 落地 `ebooks/`（累计 5 篇）。
- **索引与清单**：模块 README、MANIFEST.md（章节/时效事实/串联出边）、顶层 README 均已更新。
- **联网核实的关键事实**（2026-07 现状）：全量 GraphRAG 索引贵 6–8 倍、
  LazyGraphRAG 压至 0.1% 且全局查询省约 700 倍；多模态三路线
  （Caption / 统一嵌入 / ColPali 晚交互）；裸 Text-to-SQL 企业库准确率 50–70%、
  语义层抬至 85–95%（覆盖查询近 100%）。
