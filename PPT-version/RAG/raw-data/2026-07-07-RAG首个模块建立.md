# 原始输入留痕

- **日期**：2026-07-07
- **本次动作**：新增模块（A 类）—— 在知识库里正式建立第一个模块 RAG，从零建立整套骨架并挂入顶层索引。
- **产出**：RAG/README.html、RAG/电子书书单.md、RAG/ebooks/、RAG/RAG-讲义.pptx，以及顶层 README.html。

---

## 用户原始输入（原样留存）

> 在我的知识库里正式建第一个模块：RAG。覆盖 RAG 是什么/为什么、向量检索与 embedding、chunking 策略、重排序 reranking、常见评估方法。

## 从输入提炼出的子主题（讲义与书单据此覆盖）

1. RAG 是什么 / 为什么（What & Why）
2. 向量检索与 Embedding（Vector Retrieval & Embedding）
3. Chunking 切分策略（Chunking Strategies）
4. 重排序（Reranking）
5. 常见评估方法（RAG Evaluation）

## 路由判断

- 顶层索引此前不存在（知识库全新、空库，仅有 _skill-source/）。
- RAG 明显是一个全新主题 → 走 A 类：新建顶层索引 + 新建 RAG 模块骨架。
- RAG 是本知识库的第一个模块。

## 串联判断（第 3 步产出的一条）

- 当前库内除 RAG 外无其它模块，**暂无可真实串联的已有模块**，不虚构 MCP/Agent 等尚不存在的模块链接。
- 候选未来串联（仅作预留，不写成实链接）：
  - RAG 检索质量 ↔ 未来 Evaluation 模块的评估指标
  - RAG 的外部知识注入 ↔ 未来 Agent / MCP 模块的工具与上下文供给
  - RAG vs Fine-tuning 的取舍 ↔ 未来 Fine-tuning 模块
