---
日期：2026-07-09
本次动作：新增模块 Multimodal（A 类）。多模态是知识地图「应用模式层」预留占位（与 RAG、Agent 同层），本次正式落地。
产出 skill 版本：v3.0
---

## 用户原始输入

> 给我讲讲关于多模态的相关知识，并构建对应的知识库

（通过 /anthropic-skills:knowledge-base-builder 触发，参数即上句。）

## 输入解读

- 粗粒度输入：一个大主题「多模态（Multimodal）」，无子主题偏好，由技能提炼范围。
- 路由判定：库内无对应模块；知识地图「应用模式层」已把「多模态」列为橙色虚线占位（README.html 第 55 行）。→ A 类新增，落位应用模式层。
- 文件夹命名：沿用英文简称 `Multimodal`，与全库大小写风格一致。

## 拟定范围（待第 3 步方案细化，用户确认）

- 感知面：视觉 / 语音 / 视频 / 文档 的「理解」与「生成」两条线。
- 核心取舍：原生多模态（native/early-fusion）vs 拼管线（pipeline，OCR/ASR + LLM）。
- 关键机制：视觉编码器（ViT/CLIP）、模态对齐与融合（projection / cross-attention / Q-Former）、图像 tokenization。
- 现状盘点：主流多模态大模型格局、能力边界（时效性事实，需联网核实）。
- 上云落地：多模态推理、OCR/文档理解、语音、图像/视频生成 对应云服务。

## 串联候选（第 3 步确认）

- RAG#多模态检索（RAG 进阶章已有「多模态」节）← 双向参照。
- Agent（多模态感知让 Agent 能看屏幕/图表 → computer use）。
- LLM 原理（ViT 与 Transformer 同源；注意力机制复用）。
- LLM-Inference（多模态推理的显存与吞吐特性）。
