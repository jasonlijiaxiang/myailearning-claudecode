# 原始输入留痕

- **日期**：2026-07-07
- **本次动作**：新增模块（A 类）—— 建立 Agent 模块骨架并挂入顶层索引（顶层索引"预留扩充位"中原本就列有 Agent）。
- **产出（本步骤）**：Agent/README.html、Agent/ebooks/、Agent/raw-data/，顶层 README.html 登记新模块。
  电子书书单与讲义 PPT 待方案确认后产出。
- **完工更新（2026-07-08）**：方案经用户确认后，全部产出已落地——电子书书单.md（10 本）、
  ebooks/ 3 份官方 PDF、Agent-讲义.pptx（66 页，六章 + 全书串联/总收束，与 RAG 讲义同一设计语言），
  两级索引均已更新为 ✅。

---

## 用户原始输入（原样留存）

> 帮我看看 AI Agent 相关的

## 从输入提炼出的候选子主题（待第 3 步方案确认后定稿）

用户输入较粗，未指定子主题，以下由技能按"售前技术"定位提炼：

1. Agent 是什么 / 为什么（What & Why：LLM + 工具 + 循环；agent vs workflow vs chatbot；何时该用、何时不该用）
2. 核心组件（规划 Planning / 记忆 Memory / 工具使用 Tool Use）
3. 编排模式（单 agent、prompt chaining、routing、orchestrator-workers、multi-agent 的取舍）
4. 工具接入与 MCP（function calling 原理、MCP 协议——未来与 MCP 模块串联）
5. 上下文工程（Context Engineering：上下文窗口管理、压缩、sub-agents）
6. 评估、可观测性与安全护栏（如何评估 agent、提示注入防护、权限边界）

## 路由判断

- 读顶层索引 `myAILearning/README.html`：库内现有模块仅 RAG，无 Agent 模块。
- 顶层索引"预留扩充位"中明确列有 **Agent（智能体与编排）**，本次输入与之对应，无歧义。
- → 走 A 类：新建 Agent 模块骨架，挂入顶层索引。模块文件夹命名 `Agent`（沿用预留位写法与全库英文简称约定）。

## 串联判断（初步，第 3 步细化）

- 库内已有 RAG 模块，存在**真实可串联点**：Agent 的"工具使用"一章可回链 RAG——
  "检索"本身就是 agent 最常用的工具之一（Agentic RAG 是两模块的天然交点）。
- 候选未来串联（仅预留）：工具接入 ↔ 未来 MCP 模块；agent 评估 ↔ 未来 Evaluation 模块。
