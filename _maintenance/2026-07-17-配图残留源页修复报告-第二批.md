# 配图残留源页修复报告 · 第二批 · 2026-07-17

> 触发：用户发现 Security p30/p31 内容相同、呈现方式不同（文字卡片版 vs 信息图版），
> 要求全面检查类似问题。此为 2026-07-15 v3.8 第一批修复（14 册 62 对）的漏网批次。

## 为什么第一批会漏

信息图版与源文字页的大标题存在**全/半角标点差异**（如 `投毒:攻击者...(LLM04)` vs
`投毒：攻击者...（LLM04）`），v3.8 排查与 audit 检查项 15 都按"标题严格相同"匹配，
这批全部逃过；其中 Security 整册 7 对（含 2 对信息图改写了标题的），当时一对都没查到。

## 全库重扫（强归一化：NFKC 全半角统一 + 去标点）

9 对疑似 → 按 v3.8 铁律用**配图前基线**逐对核实（基线 1 页→现 2 页＝残留；本就 2 页＝合法）：

| 册 | 判定删除（源文字页） | 判定保留 |
| --- | --- | --- |
| Security | p6, p8, p18, p30, p38, p49, p59 共 7 页（p6/p49 为改题版，经基线相似度 1.00 + 渲染目检确认替代型） | — |
| AI-Gateway | p6, p28 共 2 页 | — |
| Data-Engineering | p35 共 1 页 | p49/50 同题两连页：配图前基线中两页均已存在（逐字节一致），为合法设计 → 登记 audit 豁免 |

共删 10 页（kb_delete zip 级），全库 1725→1715 页；四面账已回刷；19/19 audit PASS。

## 防再犯（并入 skill v3.12）

- audit 检查项 15 的标题匹配升级为 `norm_title()`（NFKC 全半角统一 + 去全部标点/空白），
  豁免清单匹配同步用同一归一化——今后此类标点变体无处可逃。
- `audit-exemptions.txt` 新增 Data-Engineering p49/50 合法两连页。

## 教训

配图管线生成信息图时会「顺手规范化标点」（半角→全角），导致图版与源页标题字符串不再相等
——任何以"字符串相等"为前提的查重，都必须先做全/半角与标点归一化。

各册删除前状态可回滚：本批删除基于当日已有的 `raw-data/history/2026-07-17-*.pptx` 之后的
可重放脚本链，且删除的 10 页内容在配图前基线（07-13/07-14 history）中完整保留。
