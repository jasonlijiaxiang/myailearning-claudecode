# 章眉统一与 XML 良构修复报告 · 2026-07-17

> 触发：用户发现 Security 正文页眉题（灰色细体 #7F8B96）与配图页眉题（青色加粗 #128199）
> 两套字体并存，要求全面检视并统一为配图页样式。

## 一、章眉样式全库扫描

19 册全部混用：配图页（07-14 批次，kb_draw 绘制）统一为 **#128199 青色加粗**；
各册正文页眉题为各自生成批次的样式——#51606E 细（11 册）、#7F8B96 细（AI-GW/Security）、
#0E4D64 细（AIC/AIP）、#7A8794 细（Eval）、#1A2733 粗（LLM）、#1B2A4A 粗（PE）。
RAG 原生即青色加粗（唯一无需处理）。另有橙色 #F2A65A 眉题 19 处（A2A×1/MCP×6/RAG×12，
对练/动手页的刻意强调色）**保留未动**。

## 二、统一执行

- zip 级批量改样式：页顶（前 8% 高度）以「第 N 章」或「来源与核实」开头的眉题文本框，
  全部 run 设为 #128199 + 加粗，字号不动。18 册共 **1105 处**。
- 各册 MANIFEST 已记一行「呈现统一」；内容与页数零变化。

## 三、执行中的事故与修复（重要教训）

首版改样式脚本的正则把 `</a:rPr>` 闭合标签**重复了一次**，1105 处全部产生非良构 XML——
LibreOffice 渲染直接丢整页（Security p15 渲染空白暴露）；而 audit 全部检查都是正则式、
不做 XML 解析，**19/19 照样 PASS**。修复：
- 全库替换 `</a:rPr></a:rPr>` → `</a:rPr>`（1105 处清零），ElementTree 全库全部件
  良构校验通过（0 错误）；
- audit 新增**检查项 17（FAIL 级）：每页 slide XML 用 ElementTree 做良构性解析**——
  字节级手术引入的标签错配从此逃不过体检（并入 skill v3.12，三处副本已同步重打包）。

**教训：任何字节级 XML 手术之后，必须过一遍真正的 XML 解析器；正则式检查对结构性
损坏是全盲的。渲染目检（本次靠它抓住空白页）依然是最后防线，不可省。**

## 四、终态验证

- 眉题样式：19 册全部 #128199 加粗（+19 处橙色强调保留）；
- audit：19/19 PASS（含新检查项 16 画布超界、17 XML 良构）；
- 页数账实：19/19 四账一致（1715 页不变）；渲染抽验 Security p14–16 正常。
